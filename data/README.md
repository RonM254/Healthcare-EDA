# Data Documentation

## Raw Data
The `data/raw/` directory contains the original, immutable data sets. This directory is gitignored to prevent committing large files or sensitive information.

## Processed Data
(Optional) If you generate processed datasets, consider adding a `data/processed/` directory.
